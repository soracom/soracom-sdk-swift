//
// Generated by SwagGen
// https://github.com/yonaskolb/SwagGen
//

import Foundation

open class _LagoonUser: Codable, Equatable {

    /** A role that represents the permission. */
    public enum Role: String, Codable {
        case viewer = "Viewer"
        case editor = "Editor"

        public static let cases: [Role] = [
          .viewer,
          .editor,
        ]
    }

    /** This value used on login. */
    open var email: String?

    open var id: Double?

    /** The last login datetime. */
    open var lastSeenAt: String?

    /** A role that represents the permission. */
    open var role: Role?

    public init(email: String? = nil, id: Double? = nil, lastSeenAt: String? = nil, role: Role? = nil) {
        self.email = email
        self.id = id
        self.lastSeenAt = lastSeenAt
        self.role = role
    }

    private enum CodingKeys: String, CodingKey {
        case email
        case id
        case lastSeenAt
        case role
    }

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        email = try container.decodeIfPresent(.email)
        id = try container.decodeIfPresent(.id)
        lastSeenAt = try container.decodeIfPresent(.lastSeenAt)
        role = try container.decodeIfPresent(.role)
    }

    open func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)

        try container.encodeIfPresent(email, forKey: .email)
        try container.encodeIfPresent(id, forKey: .id)
        try container.encodeIfPresent(lastSeenAt, forKey: .lastSeenAt)
        try container.encodeIfPresent(role, forKey: .role)
    }

    public func isEqual(to object: Any?) -> Bool {
      guard let object = object as? _LagoonUser else { return false }
      guard self.email == object.email else { return false }
      guard self.id == object.id else { return false }
      guard self.lastSeenAt == object.lastSeenAt else { return false }
      guard self.role == object.role else { return false }
      return true
    }

    public static func == (lhs: _LagoonUser, rhs: _LagoonUser) -> Bool {
        return lhs.isEqual(to: rhs)
    }
}
