//
// ExportRequest.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class ExportRequest: Codable {

    public enum Period: String, Codable { 
        case month = "month"
        case day = "day"
        case minutes = "minutes"
    }
    public var from: Int64?
    public var period: Period?
    public var to: Int64?


    
    public init(from: Int64?, period: Period?, to: Int64?) {
        self.from = from
        self.period = period
        self.to = to
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(from, forKey: "from")
        try container.encodeIfPresent(period, forKey: "period")
        try container.encodeIfPresent(to, forKey: "to")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        from = try container.decodeIfPresent(Int64.self, forKey: "from")
        period = try container.decodeIfPresent(Period.self, forKey: "period")
        to = try container.decodeIfPresent(Int64.self, forKey: "to")
    }
}

