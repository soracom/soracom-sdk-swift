//
// LagoonRegistrationRequest.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class LagoonRegistrationRequest: Codable {

    public enum Plan: String, Codable { 
        case maker = "maker"
    }
    public var plan: Plan?
    /** This password is used by the initial user&#39;s login. */
    public var userPassword: String?


    
    public init(plan: Plan?, userPassword: String?) {
        self.plan = plan
        self.userPassword = userPassword
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(plan, forKey: "plan")
        try container.encodeIfPresent(userPassword, forKey: "userPassword")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        plan = try container.decodeIfPresent(Plan.self, forKey: "plan")
        userPassword = try container.decodeIfPresent(String.self, forKey: "userPassword")
    }
}

